
<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
    <i class="fas fa-bolt"></i>
  <% end %>

  <h2><center>FlashCar</center></h2>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <li>
        <%= link_to "My Bookings", my_bookings_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
      </li>
    </ul>
  </div>
</div>

<h1><center>My Bookings<center></h1>


  <div class="container">
    <div id="cards" class="row">
      <% @bookings.each do |booking| %>
        <div class="col-4 card-container">
          <div class="card mb-4 card-car">
          <%= cl_image_tag booking.car.photo.key, height: 200, width: 360, crop: :fill %>
            <div class="card-body">
               <h1><%= booking.car.name %></h1>
              <h1 class="card-description"><%= booking.car.model %></h1>
              <h5 class="card-price"><%= booking.car.price %> CHF/J</h5>
              <h5>From:</h5>
              <p><%= booking.date_begin %></p>
              <h5>To:</h5>
              <p><%= booking.date_end %></p>
              <h3>Total Price:</h3>
              <h4 class="card-price"><%= booking.total_price %>CHF</h4>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
          <%# <center><p><%= @car.description</p> %>
    
    <%# <p><%= booking.car.name %>
    <%# <p><%= booking.car.model %>
    <%# <p><%= booking.date_begin %>
    <%# <p><%= booking.date_end %>
    <%# <p><%= booking.total_price %>
    <%# <% end %>
  
